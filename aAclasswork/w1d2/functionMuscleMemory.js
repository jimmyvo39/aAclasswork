function sayHello(name) {
    var msg = 'Hello, ' + name + '. How are you?';
    return msg;
  }
  
   console.log(sayHello('bootcamp prep')); // feel free to change the string being passed

  function sayHello(name) {
    var msg = "Hello, " + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"));//1
  
  function sayHellow(name) {
    var msg = "Hello, " + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))//2

  function sayHello(name) {
    var msg = "Hello," + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))//3

  function sayHello(name) {
    var msg = "Hello, " + name + ". How are you?";
  } return msg;

  console.log(sayHello("bootcamp prep"))//4

  function sayHello(name) {
    var msg = "Hello, " + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))// 5

  function sayHello(name) {
    var msg = "Hello, " + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))//6

  function sayHello(name) {
    var msg = "Hello," + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))//7
  
  function sayHello(name) {
    var msg = "Hello," + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))//8

  function sayHello(name) {
    var msg + "Hello," + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))//9

  function sayHello(name) {
    var msg = "Hello," + name + ". How are you?";
    return msg;
  }

  console.log(sayHello("bootcamp prep"))//10
  