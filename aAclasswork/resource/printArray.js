function printArray(arr) {
    for (var i = 0; i < arr.length; i += 1) {
      var ele = arr[i];
      console.log(ele)
    }
  }
  
  /*
 for loop iterated through every index
the condition is flexible for all array lengths
the last index is always at array.length - 1 hense i < array.length
each element will print


*/