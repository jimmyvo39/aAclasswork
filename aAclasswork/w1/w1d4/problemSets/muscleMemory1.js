function printForwards(arr) {
    for (var i = 0; i < arr.length; i++) {
      var ele = arr[i];
      console.log(ele);
    }
  }
  
  var alphabet = ['a', 'b', 'c', 'd', 'e'];
  printForwards(alphabet);

function printForwards(arr) {
  for (var i = 0; i < arr.length; i++) {
    var ele = arr[i];
    console.log(ele);
  }
}

  var alphabet = ["a", "b", "c", "d", "e"];
  printForwards(alphabet);//1

  function printForwards(arr) {
    for (var i = 0; i < arr.length; i += 1) {
      var ele = arr[i];
      console.log(ele);
    }
  }

  var alphabet = ["a", "b", "c,", "d", "e"]
  printForwards(alphabet);//2

  function printForwards(arr) {
    for (var i = 0; i < arr.length; i += 1) {
      var ele = arr[i]
      console.log(ele)
    }
  }

  var alphabet = ["a", "b", "c", "d", "e"]
  printForwards(alphabet);//3

  function printForwards(arr) {
    for (var i = 0; i < arr.length; i += 1 ) {
      var ele = arr[i];
      console.log(ele)
    }
  }

  var alphabet = ["a", "b", "c", "d", "e"]
  printForwards(alphabet);//4

  function printForwards(arr) {
    for (var i = 0; i < arr.length; i += 1) {
      var ele = arr[i]
      console.log(ele)
    }
  }

  var alphabet = ["a", "b", "c", "d", "e"];
  printForwards(alphabet);//5