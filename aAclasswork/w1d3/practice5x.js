function printForwards(start, end) {
    for (var counter = start; counter <= end; counter++) {
      console.log(counter);
    }
  }
  
  printForwards(4, 11); // feel free to change the numbers being passed



function printForwards(start, end) {
    for (var counter = start; counter <= end; counter++) {
        console.log(counter);
    }
}

printForwards(4, 11);//1

function printForwards(start, end) {
    for (var counter = start; counter <= end; counter++) {
        console.log(counter);
    }
}

printForwards(4, 11);//2

function printForwards(start, end) {
    for (var counter = start; counter <= end; counter++) {
        console.log(counter)
    }
}

printForwards(4, 11);//3

function printForwards(start, End) {
    for (var counter = start; counter <= end; counter++) {
        console.log(counter)
    }
}

printForwards(4, 11);//4

function printForwards(start, end) {
    for (var counter = start; counter <= end; Counter++) {
        console.log(counter)
    }
}

printForwards(4, 11);//5

//--------------------------------------------------------

function printBackwards(start, end) {
   var i = end;
   while (i >= start) {
     console.log(i);
     i--;
    }
}
  
printBackwards(-5, 6); // feel free to change the numbers being passed 

function printBackwards(start, end) {
    var i =end;
    while (i >= start) {
        console.log(i);
        i--;
    }
}

printBackwards(-5, 6)//1

function printBackwards(start, end) {
    var i= end;
    while (i >= start) {
        console.log(i);
        i--;
    }
}

printBackwards(-5, 6)//2

function printBackwards(start, end) {
    var i = end;
    while (i >= start) {
        console.log(i);
        i--;
    }
}

printBackwards(-5, 6);//3

function printBackwards(start, end) {
    var i = end;
    while (i >= start) {
        console.log(i);
        i--;
    }
}

printBackwards(-5, 6);//4

function printBackwards(start, end) {
    var i = end;
    while (i >= start) {
        console.log(i);
        i--;
    }
}

printBackwards(-5, 6);//5